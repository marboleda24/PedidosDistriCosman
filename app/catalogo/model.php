<?php
/*
require_once 'funciones.php';
require '../../all/conexion/conexion.php';

$session_id= session_id();

#session_start();

$tmp = new FuncionesCatalogo();

$session_id = $_POST['session_id'];
$id = $_POST['id'];
$titulo = $_POST['titulo'];
$referencia = $_POST['referencia'];
$valor = $_POST['valor'];
$cantidad = $_POST['cantidad'];

#insertamos los datos en la base de datos temporal

$tmp -> tmp($conexion, $id, $titulo, $cantidad, $valor, $referencia, $session_id);

echo true;*/